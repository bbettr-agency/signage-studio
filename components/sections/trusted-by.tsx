import LogoMarquee from "./logo-marquee";

/**
 * TrustedBy has been refreshed to a seamless logo marquee.
 * The homepage keeps importing this same export path so nothing else changes.
 */
export default function TrustedBy() {
  return <LogoMarquee />;
}
